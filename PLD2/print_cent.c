#include "main.h"

/**
 * print_cent: prints the % character to stdout
 * @args: variadic parameters (va_list). NB: this may be
 * cast to void since it isn't useful yet
 *
 * Return: always 1
 */

int print_cent(va_list args)
{
//	(void)args;

//	write(1, '%', 1);
//	return (1);
}
